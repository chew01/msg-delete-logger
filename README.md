# msg-delete-logger
When you've had enough of people deleting stuff

## Slash commands
Set `TOKEN` and `CLIENT_ID` in your environment, then deploy globally:

`bun run deploy:commands`

Global command updates can take up to about an hour to fully propagate.
